[build-system]
requires = [
  "setuptools>=61",
  "wheel",
  "torch",
]
build-backend = "setuptools.build_meta"

[project]
name = "ltx-kernels"
version = "0.1.0"
dependencies = ["torch"]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
